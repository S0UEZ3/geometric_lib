# Square of a
def area(a):
    if a < 0:
        raise AssertionError("Error: argument should be positive")
    return a * a

# Product of 4 and a


def perimeter(a):
    if a < 0:
        raise AssertionError("Error: argument should be positive")
    return 4 * a
